class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        array = []
        for num in nums:
            array.append(num)
        for num in nums:
            array.append(num)
            
        return array
        